recording_ms <= trip_duration_ms
AND connected_ms <= trip_duration_ms
AND org_id IS NOT NULL
AND auxcam_id IS NOT NULL
AND vg_id IS NOT NULL
AND connected_ms IS NOT NULL
AND recording_ms IS NOT NULL
AND trip_start_ms IS NOT NULL
AND trip_duration_ms IS NOT NULL
AND trip_start_ms != 0
