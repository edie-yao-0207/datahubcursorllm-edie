`_timestamp` TIMESTAMP,
`_filename` STRING,
`_rowid` STRING,
`_op` STRING,
`org_id` BIGINT,
`id` BIGINT,
`name` STRING,
`created_by` BIGINT,
`created_at` TIMESTAMP,
`updated_at` TIMESTAMP,
`config_proto` STRUCT<
  `labels`: MAP<STRING, STRING>,
  `units`: INT,
  `custom_units`: STRING,
  `start_ms`: BIGINT,
  `end_ms`: BIGINT,
  `smooth_ms_range`: BIGINT,
  `weight_factor`: BIGINT,
  `user_read_only`: BOOLEAN,
  `rpm`: DOUBLE,
  `cloned_input_id`: BIGINT
>,
`_raw_config_proto` STRING,
`type_id` BIGINT,
`asset_uuid` STRING,
`format` INT,
`is_template` BYTE,
`template_data_input_id` BIGINT,
`partition` STRING
