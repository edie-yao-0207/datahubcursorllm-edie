`_timestamp` TIMESTAMP,
`_filename` STRING,
`_rowid` STRING,
`_op` STRING,
`org_id` BIGINT,
`device_id` BIGINT,
`event_ms` BIGINT,
`user_id` BIGINT,
`created_at` BIGINT,
`detail_proto` STRUCT<
  `is_useful`: BOOLEAN,
  `not_useful_reason`: INT,
  `other_reason`: STRING,
  `is_dismissed_through_survey`: BOOLEAN
>,
`_raw_detail_proto` STRING,
`harsh_accel_type` SHORT,
`firmware_detected_event` BYTE,
`user_type` SHORT,
`date` STRING
