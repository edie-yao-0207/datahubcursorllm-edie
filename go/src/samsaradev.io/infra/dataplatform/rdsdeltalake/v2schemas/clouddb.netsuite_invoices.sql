`_timestamp` TIMESTAMP,
`_filename` STRING,
`_rowid` STRING,
`_op` STRING,
`sam_number` STRING,
`proto_data` STRUCT<
  `currency_override`: STRING,
  `subtotal_cents`: BIGINT,
  `tax_cents`: BIGINT,
  `shipping_cost_cents`: BIGINT,
  `amount_remaining_cents`: BIGINT,
  `invoice_status`: STRING,
  `due_date_ms`: BIGINT,
  `payment_status`: INT,
  `po_number`: STRING,
  `order_number`: STRING,
  `url`: STRING,
  `status`: INT,
  `total_cents`: BIGINT,
  `billing_country`: STRING,
  `billing_attention`: STRING,
  `billing_addressee`: STRING,
  `billing_phone`: STRING,
  `billing_address_1`: STRING,
  `billing_address_2`: STRING,
  `billing_city`: STRING,
  `billing_state`: STRING,
  `billing_zip`: STRING,
  `shipping_country`: STRING,
  `shipping_attention`: STRING,
  `shipping_addressee`: STRING,
  `shipping_phone`: STRING,
  `shipping_address_1`: STRING,
  `shipping_address_2`: STRING,
  `shipping_city`: STRING,
  `shipping_state`: STRING,
  `shipping_zip`: STRING,
  `consolidated_invoice_id`: BIGINT,
  `invoice_number_str`: STRING,
  `charge_error_message`: STRING,
  `itemized_report_status`: STRING
>,
`_raw_proto_data` STRING,
`netsuite_internal_id` BIGINT,
`created_at` TIMESTAMP,
`date` STRING
