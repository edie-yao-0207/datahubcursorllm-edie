`date` STRING,
`stat_type` INT,
`org_id` BIGINT,
`object_type` INT,
`object_id` BIGINT,
`time` BIGINT,
`value` STRUCT<
  `date`: STRING,
  `time`: BIGINT,
  `received_delta_seconds`: BIGINT,
  `is_start`: BOOLEAN,
  `is_end`: BOOLEAN,
  `is_databreak`: BOOLEAN,
  `int_value`: BIGINT,
  `double_value`: DOUBLE,
  `proto_value`: STRUCT<
    `tachograph_metadata`: STRUCT<
      `supplier_id_valid`: BOOLEAN,
      `supplier_id`: STRING,
      `part_number_valid`: BOOLEAN,
      `part_number`: STRING,
      `card_writing_enabled_valid`: BOOLEAN,
      `card_writing_enabled`: BOOLEAN,
      `rdl_can_a_enabled_valid`: BOOLEAN,
      `rdl_can_a_enabled`: BOOLEAN,
      `rdl_can_c_enabled_valid`: BOOLEAN,
      `rdl_can_c_enabled`: BOOLEAN,
      `tachograph_vehicle_registration`: STRING,
      `card_type_slot_1`: INT,
      `card_id_slot_1`: STRING,
      `card_type_slot_2`: INT,
      `card_id_slot_2`: STRING,
      `uds_driver_1_rest_time`: STRUCT<
        `remaining_current_driving_time_minute_valid`: BOOLEAN,
        `remaining_current_driving_time_minute`: BIGINT,
        `remaining_time_until_next_break_or_rest_minute_valid`: BOOLEAN,
        `remaining_time_until_next_break_or_rest_minute`: BIGINT,
        `duration_of_next_break_rest_minute_valid`: BOOLEAN,
        `duration_of_next_break_rest_minute`: BIGINT,
        `remaining_time_of_current_break_rest_minute_valid`: BOOLEAN,
        `remaining_time_of_current_break_rest_minute`: BIGINT,
        `time_left_until_next_driving_period_minute_valid`: BOOLEAN,
        `time_left_until_next_driving_period_minute`: BIGINT,
        `duration_of_next_driving_period_minute_valid`: BOOLEAN,
        `duration_of_next_driving_period_minute`: BIGINT,
        `current_daily_driving_time_minute_valid`: BOOLEAN,
        `current_daily_driving_time_minute`: BIGINT,
        `time_left_until_new_daily_rest_period_minute_valid`: BOOLEAN,
        `time_left_until_new_daily_rest_period_minute`: BIGINT,
        `minimum_daily_rest_minute_valid`: BOOLEAN,
        `minimum_daily_rest_minute`: BIGINT,
        `remaining_driving_time_of_current_week_minute_valid`: BOOLEAN,
        `remaining_driving_time_of_current_week_minute`: BIGINT,
        `time_left_until_new_weekly_rest_period_minute_valid`: BOOLEAN,
        `time_left_until_new_weekly_rest_period_minute`: BIGINT,
        `minimum_weekly_rest_minute_valid`: BOOLEAN,
        `minimum_weekly_rest_minute`: BIGINT,
        `open_compensation_in_the_last_week_minute_valid`: BOOLEAN,
        `open_compensation_in_the_last_week_minute`: BIGINT,
        `open_compensation_in_week_before_last_minute_valid`: BOOLEAN,
        `open_compensation_in_week_before_last_minute`: BIGINT,
        `open_compensation_in_second_week_before_last_minute_valid`: BOOLEAN,
        `open_compensation_in_second_week_before_last_minute`: BIGINT,
        `end_of_last_daily_rest_period_valid`: BOOLEAN,
        `end_of_last_daily_rest_period`: STRUCT<
          `tachograph_timestamp_unix_secs`: BIGINT,
          `tachograph_local_offset_secs`: INT
        >,
        `maximum_daily_driving_time_minute_valid`: BOOLEAN,
        `maximum_daily_driving_time_minute`: BIGINT,
        `number_of_times_9h_daily_driving_times_exceeded_valid`: BOOLEAN,
        `number_of_times_9h_daily_driving_times_exceeded`: BIGINT,
        `number_of_used_reduced_daily_rest_periods_valid`: BOOLEAN,
        `number_of_used_reduced_daily_rest_periods`: BIGINT,
        `remaining_2_weeks_driving_time_valid`: BOOLEAN,
        `remaining_2_weeks_driving_time`: BIGINT
      >,
      `uds_driver_2_rest_time`: STRUCT<
        `remaining_current_driving_time_minute_valid`: BOOLEAN,
        `remaining_current_driving_time_minute`: BIGINT,
        `remaining_time_until_next_break_or_rest_minute_valid`: BOOLEAN,
        `remaining_time_until_next_break_or_rest_minute`: BIGINT,
        `duration_of_next_break_rest_minute_valid`: BOOLEAN,
        `duration_of_next_break_rest_minute`: BIGINT,
        `remaining_time_of_current_break_rest_minute_valid`: BOOLEAN,
        `remaining_time_of_current_break_rest_minute`: BIGINT,
        `time_left_until_next_driving_period_minute_valid`: BOOLEAN,
        `time_left_until_next_driving_period_minute`: BIGINT,
        `duration_of_next_driving_period_minute_valid`: BOOLEAN,
        `duration_of_next_driving_period_minute`: BIGINT,
        `current_daily_driving_time_minute_valid`: BOOLEAN,
        `current_daily_driving_time_minute`: BIGINT,
        `time_left_until_new_daily_rest_period_minute_valid`: BOOLEAN,
        `time_left_until_new_daily_rest_period_minute`: BIGINT,
        `minimum_daily_rest_minute_valid`: BOOLEAN,
        `minimum_daily_rest_minute`: BIGINT,
        `remaining_driving_time_of_current_week_minute_valid`: BOOLEAN,
        `remaining_driving_time_of_current_week_minute`: BIGINT,
        `time_left_until_new_weekly_rest_period_minute_valid`: BOOLEAN,
        `time_left_until_new_weekly_rest_period_minute`: BIGINT,
        `minimum_weekly_rest_minute_valid`: BOOLEAN,
        `minimum_weekly_rest_minute`: BIGINT,
        `open_compensation_in_the_last_week_minute_valid`: BOOLEAN,
        `open_compensation_in_the_last_week_minute`: BIGINT,
        `open_compensation_in_week_before_last_minute_valid`: BOOLEAN,
        `open_compensation_in_week_before_last_minute`: BIGINT,
        `open_compensation_in_second_week_before_last_minute_valid`: BOOLEAN,
        `open_compensation_in_second_week_before_last_minute`: BIGINT,
        `end_of_last_daily_rest_period_valid`: BOOLEAN,
        `end_of_last_daily_rest_period`: STRUCT<
          `tachograph_timestamp_unix_secs`: BIGINT,
          `tachograph_local_offset_secs`: INT
        >,
        `maximum_daily_driving_time_minute_valid`: BOOLEAN,
        `maximum_daily_driving_time_minute`: BIGINT,
        `number_of_times_9h_daily_driving_times_exceeded_valid`: BOOLEAN,
        `number_of_times_9h_daily_driving_times_exceeded`: BIGINT,
        `number_of_used_reduced_daily_rest_periods_valid`: BOOLEAN,
        `number_of_used_reduced_daily_rest_periods`: BIGINT,
        `remaining_2_weeks_driving_time_valid`: BOOLEAN,
        `remaining_2_weeks_driving_time`: BIGINT
      >,
      `uds_driver_1_working_state`: INT,
      `uds_driver_2_working_state`: INT,
      `uds_tachograph_time_date_valid`: BOOLEAN,
      `uds_tachograph_time_date`: STRUCT<
        `tachograph_timestamp_unix_secs`: BIGINT,
        `tachograph_local_offset_secs`: INT
      >,
      `next_cal_date_year`: BIGINT,
      `next_cal_date_month`: BIGINT,
      `next_cal_date_day`: BIGINT,
      `next_cal_date_valid`: BOOLEAN
    >
  >
>,
`_filename` STRING,
`_sort_key` STRING
