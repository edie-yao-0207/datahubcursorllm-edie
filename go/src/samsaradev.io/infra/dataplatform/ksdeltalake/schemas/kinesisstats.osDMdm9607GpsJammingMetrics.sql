`date` STRING,
`stat_type` INT,
`org_id` BIGINT,
`object_type` INT,
`object_id` BIGINT,
`time` BIGINT,
`value` STRUCT<
  `date`: STRING,
  `time`: BIGINT,
  `received_delta_seconds`: BIGINT,
  `is_start`: BOOLEAN,
  `is_end`: BOOLEAN,
  `is_databreak`: BOOLEAN,
  `int_value`: BIGINT,
  `double_value`: DOUBLE,
  `proto_value`: STRUCT<
    `mdm9607_gps_jamming_metrics`: STRUCT<
      `pga_gain`: INT,
      `adc_amplitude_i`: BIGINT,
      `adc_amplitude_q`: BIGINT,
      `jammer_metric_gps_db`: INT,
      `jammer_metric_glo_db`: INT,
      `jammer_metric_bds_db`: INT,
      `jammer_metric_gal_db`: INT,
      `agc_metric_gps_millidb`: INT,
      `agc_metric_glo_millidb`: INT,
      `agc_metric_bds_millidb`: INT,
      `agc_metric_gal_millidb`: INT,
      `gps_baseband_processor_amplitude_i`: BIGINT,
      `gps_baseband_processor_amplitude_q`: BIGINT,
      `avg_glo_baseband_processor_amplitude_i`: BIGINT,
      `avg_glo_baseband_processor_amplitude_q`: BIGINT,
      `bds_baseband_processor_amplitude_i`: BIGINT,
      `bds_baseband_processor_amplitude_q`: BIGINT,
      `gal_baseband_processor_amplitude_i`: BIGINT,
      `gal_baseband_processor_amplitude_q`: BIGINT,
      `is_reference`: BOOLEAN,
      `gps_week_number`: BIGINT,
      `gps_time_of_week_milliseconds`: BIGINT,
      `time_valid`: BOOLEAN,
      `time_source`: BIGINT,
      `time_uncertainty_microseconds`: INT,
      `clock_freq_bias_ppb`: INT,
      `clock_freq_bias_uncertainty_ppb`: INT,
      `oscillator_state`: INT,
      `leap_seconds`: INT,
      `leap_second_uncertainty_seconds`: INT,
      `log_reason_bitmask`: BIGINT
    >
  >
>,
`_filename` STRING,
`_sort_key` STRING
