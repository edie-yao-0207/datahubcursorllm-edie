`date` STRING,
`stat_type` INT,
`org_id` BIGINT,
`object_type` INT,
`object_id` BIGINT,
`time` BIGINT,
`value` STRUCT<
  `date`: STRING,
  `time`: BIGINT,
  `received_delta_seconds`: BIGINT,
  `is_start`: BOOLEAN,
  `is_end`: BOOLEAN,
  `is_databreak`: BOOLEAN,
  `int_value`: BIGINT,
  `double_value`: DOUBLE,
  `proto_value`: STRUCT<
    `cm3x_power_stats`: STRUCT<
      `usb_input_current_now_ua`: FLOAT,
      `usb_voltage_now_uv`: FLOAT,
      `battery_voltage_now_uv`: FLOAT,
      `battery_current_now_ua`: FLOAT,
      `battery_temp_dc`: FLOAT,
      `battery_charger_temp_dc`: FLOAT,
      `battery_health`: STRING,
      `battery_status`: STRING,
      `usb_system_power_now_uw`: DECIMAL(20, 0),
      `usb_detect_status`: BOOLEAN,
      `vehicle_current_now_ua`: FLOAT,
      `vehicle_voltage_now_uv`: FLOAT,
      `vehicle_power_now_uw`: FLOAT,
      `camera1_current_now_ua`: FLOAT,
      `camera2_current_now_ua`: FLOAT,
      `camera3_current_now_ua`: FLOAT,
      `camera4_current_now_ua`: FLOAT,
      `display_current_now_ua`: FLOAT,
      `bms_capacity_pcnt`: FLOAT,
      `bms_capacity_raw`: FLOAT,
      `bms_resistance_uohm`: FLOAT,
      `bms_charge_counter_uahr`: FLOAT,
      `bms_charge_counter_shadow_uahr`: FLOAT,
      `bms_voltage_ocv_uv`: FLOAT,
      `bms_time_to_full_avg_s`: FLOAT,
      `bms_time_to_empty_avg_s`: FLOAT,
      `bms_charge_now_uahr`: FLOAT,
      `bms_charge_now_raw_uahr`: FLOAT,
      `bms_charge_full_uahr`: FLOAT
    >
  >
>,
`_filename` STRING,
`_sort_key` STRING
