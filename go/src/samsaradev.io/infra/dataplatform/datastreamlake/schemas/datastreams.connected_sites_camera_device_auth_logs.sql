`key` STRING,
`org_id` BIGINT,
`discovery_name` STRING,
`mac_address` STRING,
`mac_address_int64` BIGINT,
`vms_id` BIGINT,
`encrypted_username` STRING,
`encrypted_password` STRING,
`firmware_version` STRING,
`manufacturer` STRING,
`hardware_id` STRING,
`model` STRING,
`serial_number` STRING,
`x_addr` STRING,
`ip_address` STRING,
`hostname` STRING,
`onvif_supported` BOOLEAN,
`axis_supported` BOOLEAN,
`open_rtsp_port` BOOLEAN,
`open_http_port` BOOLEAN,
`ip_range_scanned` BOOLEAN,
`manually_added` BOOLEAN,
`auto_authed` BOOLEAN,
`force_added` BOOLEAN,
`mac_found_method` STRING,
`manufacturer_found_method` STRING,
`camera_api_types` ARRAY<STRING>,
`camera_auth_statuses` ARRAY<STRING>,
`authed` BOOLEAN,
`previously_added` BOOLEAN,
`user_interacted` BOOLEAN,
`user_auth_attempts` BIGINT,
`in_site_gateway_subnet` STRING,
`user_id` BIGINT,
`is_samsara_admin` BOOLEAN,
`cell` STRING,
`metric_version` BIGINT,
`session_uuid` STRING,
`date` DATE
