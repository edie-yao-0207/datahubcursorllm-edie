`stream_page_session_uuid` STRING,
`stream_id` BIGINT,
`org_id` BIGINT,
`user_id` BIGINT,
`remote_ip_address` STRING,
`proxy_ip_address` STRING,
`server_ip_address` STRING,
`user_agent` STRING,
`start_ms` BIGINT,
`duration_ms` BIGINT,
`is_live` BOOLEAN,
`resolution` STRING,
`streaming_state` STRING,
`view_mode` STRING,
`playback_speed` STRING,
`audio_muted` BOOLEAN,
`has_audio` BOOLEAN,
`video_datapoint_json_strings` ARRAY<STRING>,
`live_video_latency_datapoint_json_strings` ARRAY<STRING>,
`date` DATE
