`timestamp_ms` BIGINT,
`s3_key` STRING,
`gps_speed_mps` DOUBLE,
`ecu_speed_mps` DOUBLE,
`latitude` DOUBLE,
`longitude` DOUBLE,
`heading_degrees` DOUBLE,
`way_id` BIGINT,
`osm_road_class` STRING,
`distance_meters_from_way` DOUBLE,
`skipped` BOOLEAN,
`skipped_reason` STRING,
`model_version` BIGINT,
`date` DATE
