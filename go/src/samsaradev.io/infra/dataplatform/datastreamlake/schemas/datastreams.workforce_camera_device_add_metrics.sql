`id` BIGINT,
`key` STRING,
`org_id` BIGINT,
`name` STRING,
`mac_address` STRING,
`mac_address_int64` BIGINT,
`vms_id` BIGINT,
`encrypted_username` STRING,
`encrypted_password` STRING,
`firmware_version` STRING,
`manufacturer` STRING,
`hardware_id` STRING,
`model` STRING,
`serial_number` STRING,
`x_addr` STRING,
`ip_address` STRING,
`hostname` STRING,
`created_at` TIMESTAMP,
`onvif_supported` BOOLEAN,
`axis_supported` BOOLEAN,
`open_rtsp_port` BOOLEAN,
`open_http_port` BOOLEAN,
`ip_range_scanned` BOOLEAN,
`manually_added` BOOLEAN,
`auto_authed` BOOLEAN,
`force_added` BOOLEAN,
`mac_found_method` STRING,
`manufacturer_found_method` STRING,
`camera_api_types` ARRAY<STRING>,
`in_site_gateway_subnet` STRING,
`user_id` BIGINT,
`cell` STRING,
`metric_version` BIGINT,
`camera_add_session_uuid` STRING,
`date` DATE
