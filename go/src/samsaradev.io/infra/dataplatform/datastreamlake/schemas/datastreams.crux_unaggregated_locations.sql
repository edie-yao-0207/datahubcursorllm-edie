`timestamp` TIMESTAMP,
`peripheral_org_id` BIGINT,
`peripheral_device_id` BIGINT,
`latitude` DOUBLE,
`longitude` DOUBLE,
`accuracy_millimeters` BIGINT,
`in_same_org` BOOLEAN,
`rssi` INT,
`source_network` STRING,
`source_org_id` BIGINT,
`source_device_id` BIGINT,
`source_object_type` INT,
`source_device_speed_meters_per_second` DOUBLE,
`location_received_at` BIGINT,
`request_received_at` BIGINT,
`timestamps_of_locations_used_for_inference` ARRAY<BIGINT>,
`reported_tx_power` INT,
`algorithm` STRING,
`time_since_last_motion` INT,
`date` DATE
