`timestamp` TIMESTAMP,
`org_id` BIGINT,
`device_id` BIGINT,
`octo_device_id` BIGINT,
`start_ms` BIGINT,
`end_ms` BIGINT,
`is_hyperlapse` BOOLEAN,
`is_lowres` BOOLEAN,
`camera_streams` ARRAY<STRING>,
`media_stream_sources` ARRAY<STRING>,
`data_streams` ARRAY<STRING>,
`success` BOOLEAN,
`job_id` STRING,
`debug_metadata` STRING,
`skip_reason` STRING,
`video_retrieval_id` BIGINT,
`request_source` STRING,
`request_uuid` STRING,
`client_asset_id` STRING,
`trigger_type` STRING,
`signal_source` STRING,
`date` DATE
