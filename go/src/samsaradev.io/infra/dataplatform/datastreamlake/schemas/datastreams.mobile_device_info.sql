`timestamp` TIMESTAMP,
`device_uuid` STRING,
`platform` STRING,
`os_version` STRING,
`model` STRING,
`bundle_identifier` STRING,
`native_build_number` BIGINT,
`codepush_build_number` BIGINT,
`event_uuid` STRING,
`session_uuid` STRING,
`date` DATE
